<metro:Window x:Class="DriftPlayer.AddToPlaylistDialogBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:metro="http://schemas.codeplex.com/elysium" 
        xmlns:params="http://schemas.codeplex.com/elysium/params"
        params:Manager.Theme="Dark" 
        params:Design.Theme="Dark" WindowStartupLocation="CenterScreen"
        Title="Přidat do playlistu" Height="100" Width="500" ResizeMode="NoResize">
    <Grid>
        <Grid Margin="20" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <TextBox x:Name="videoBox" HorizontalAlignment="Left" TextWrapping="NoWrap" VerticalAlignment="Stretch" FontSize="16" Tag="Zadejte url..." Style="{StaticResource WatermarkTextBox}" Foreground="#FFFFFCFC" Margin="0,0,0,-1" Width="372"/>
            <Button x:Name="addButton" HorizontalAlignment="Right" VerticalAlignment="Stretch" Width="75" Click="addButton_Click" Margin="377,0,0,0">
                <Image Source="/Images/arrow_right.png"/>
            </Button>
        </Grid>
    </Grid>
</metro:Window>
